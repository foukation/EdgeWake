# ===========================================================================
# 编库工程入口组件（空壳）
# ===========================================================================
# 该 main 组件只提供空的 app_main()，用于满足 ESP-IDF 构建系统。
# 真正的唤醒 SDK 在 components/wake_word_detection_sdk。
# ===========================================================================
idf_component_register(
    SRCS "main.c"
    INCLUDE_DIRS "")
