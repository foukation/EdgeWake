# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
#org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# ---------------------------------------------------------------------------
# 编译内存调优（修复 native malloc OOM 崩溃，见 hs_err_pid*.log）
# 现象：编译约 59s 时 JVM 报 "Native memory allocation (malloc) failed"，
#       崩在 C2 CompilerThread —— 是 native 堆内存耗尽，而非 Java 堆不足。
# 原因：15G 内存机器上，原 -Xmx4096m 的 Java 堆预留过大，挤占了 native 堆的
#       增长空间（compressed oops 下 Java 堆开在低地址，会限制 native 堆上限）。
# ---------------------------------------------------------------------------
# -Xmx3072m           ：Java 堆由 4096m 调小到 3072m，把内存让给 native 堆（JVM 日志的头号建议）。
# -XX:MaxMetaspaceSize：替换 JDK11 已失效的 -XX:MaxPermSize=1024m，限制元空间上限为 512m。
org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
# 关闭并行构建：多模块同时编译会成倍放大内存峰值，串行更稳（本项目为 fat-aar 聚合，收益有限）。
org.gradle.parallel=false
# 限制并发 worker 数为 2：进一步压低同一时刻的内存占用。
org.gradle.workers.max=2
# 限制 Kotlin 独立编译 daemon 的堆上限，避免它与 Gradle daemon 争抢内存。
kotlin.daemon.jvmargs=-Xmx1536m
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
android.enableJetifier=true
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
android.disableAutomaticComponentCreation=true
COMPILE_SDK_VERSION=31
BUILD_SDK_VERSION=28.0.3
MIN_SDK_VERSION=21
#TARGET_SDK_VERSION=26
TARGET_SDK_VERSION=28
MAGIC_TOOL_VERSION=3.0.0.000
MAGIC_TOOL_IS_SNAPSHOT=false
KOTLIN_VERSION=1.7.10
DUEROS_ACL_VERSION=1.7.5.2-5754
AROUTER_API_VERSION=1.3.1
AROUTER_COMPILER_VERSION=1.1.4
EVENTBUS_VERSION=3.2.0
GSON_VERSION=2.9.0
GLIDE_VERSION=4.12.0
OKHTTP_VERSION=3.12.12
DCS_VERSION=1.7.5.2-5700
SUPPORT_VERSION=27.1.1
ANDROIDX_APPCOMPAT=1.3.0
ANDROIDX_CARDVIEW=1.0.0
GOOGLE_MATERIAL=1.3.0
android.injected.testOnly=false
# SDK version
SDK_VERSION="ai_1.1.7"
#SDK_VERSION_FULLY_COMPATIBLE="ai_1.0.1_fullyCompatible"
#SDK_VERSION_LIGHT_WEIGHT_ADAPTATION="ai_1.0.1_lightweightAdaptation"