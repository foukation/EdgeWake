# ===========================================================================
# AI SDK 独立编译项目 - main 组件配置
# ===========================================================================
#
# 该文件定义 main 组件，作为 ESP-IDF 项目的入口点。
#
# 说明：
# - ESP-IDF 项目必须有 main 组件
# - 这里只包含一个空的 app_main() 函数
# - 通过 REQUIRES ai_sdk 触发 AI SDK 的编译
#
# ===========================================================================

idf_component_register(
    # 源文件
    SRCS "main.c"
    
    # 头文件目录（无）
    INCLUDE_DIRS ""
    
    # 依赖 ai_sdk 组件，触发其编译
    REQUIRES ai_sdk
)

