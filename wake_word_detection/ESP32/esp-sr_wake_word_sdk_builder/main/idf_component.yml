# ===========================================================================
# Wake Word SDK Builder 测试程序依赖声明
# ===========================================================================

dependencies:
  # ESP-IDF 版本要求
  idf:
    version: '>=5.4.0'
  
  # ESP-SR 语音识别组件
  espressif/esp-sr: ~2.2.0
  
  # Opus 编码器组件
  78/esp-opus-encoder: ~2.4.1
